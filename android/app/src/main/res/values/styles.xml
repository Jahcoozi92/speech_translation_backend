<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- LaunchTheme -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <!-- NormalTheme -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <!-- AppTheme -->
    <style name="Theme.EchtzeitUebersetzer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primäre Farben -->
        <item name="colorPrimary">@color/teal_700</item>
        <item name="colorSecondary">@color/orange_accent</item>

        <!-- Schriftfamilie -->
        <item name="android:fontFamily">@font/fonts</item>

        <!-- Weitere Anpassungen... -->
    </style>

    <!-- Weitere App-spezifische Styles -->
    <style name="Theme.MyApp" parent="Theme.Material3.DayNight">
        <item name="textAppearanceHeadlineMedium">@style/TextAppearance.MyApp.HeadlineSmall</item>
    </style>

    <style name="TextAppearance.MyApp.HeadlineSmall"
        parent="TextAppearance.Material3.HeadlineMedium">
        <!-- Anpassen von Eigenschaften wie Schriftfamilie, Größe, Farbe usw. -->
    </style>
</resources>
